std::cin 表示从标准输入中读取内容
作用域操作符(::)的含义:编译器应聪哥操作符左侧名字所示的作用域中寻找右侧那个名字
有了using 声明就无须专门的前缀

每个名字都需要独立的using声明:按照规定,每个using声明引入命名空间的一个成员.
eg.:
    using std::cin;
    using std::cout; using std::endl; //每个名字都必须有自己的声明语句,而且每句话都得以分号结束.
    
头文件不应包含using声明
